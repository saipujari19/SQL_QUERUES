select '              sai'

select trim('              sai') -- which deletes the empty spaces at the beginning

select 'HI WELCOME TO SQL'

select lower('HI WELCOME TO SQL') -- converts into lower case

select 'hi welcome to sql'

select upper('hi welcome to sql') -- converts into upper case

select reverse('hi welcome to sql') --which reverse the string 

select 'hello moto'

select substring('hello moto',7,5)-- we nned 3 parameters, the string index(count even the spaces) and length of the substring.


